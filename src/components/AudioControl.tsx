// AudioControl - DEPRECATED
// Replaced by BackgroundMusicControl which uses custom music.
// This component renders nothing to avoid duplicate audio controls.
const AudioControl = () => null;
export default AudioControl;
